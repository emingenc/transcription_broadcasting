{
  "name": "mentra-cast",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "bun src/index.ts",
    "dev": "bun --hot src/index.ts",
    "docker:build": "docker build -f docker/Dockerfile -t mentra-cast .",
    "docker:up": "docker-compose -f docker/docker-compose.yml up",
    "docker:down": "docker-compose -f docker/docker-compose.yml down"
  },
  "author": "",
  "license": "ISC",
  "description": "Real-time broadcast subtitles for Mentra smart glasses",
  "dependencies": {
    "@mentra/sdk": "^2.1.29",
    "express": "^4.21.2",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/express": "^5.0.1",
    "@types/node": "^22.13.14"
  }
}
